<?wtrjXml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jin.admin.dao.mapper.SysMenuMapper">

    <select id="getMenuByUserId" resultType="com.jin.admin.response.MenuData">
        SELECT
            m.*, mr.sort_num
        FROM
        sys_menu m
        LEFT JOIN sys_menu_sort_rel mr ON mr.menu_id = m.id
        <if test="userId != null and userId != 1">
            LEFT JOIN sys_permission_resource_rel rr ON rr.resource_id = m.id
            LEFT JOIN sys_permission p ON rr.permission_id = p.id
            LEFT JOIN sys_role_permission_rel pr ON pr.permission_id = p.id
            LEFT JOIN sys_role r ON r.id = pr.role_id
            LEFT JOIN sys_user_role_rel ur ON r.id = ur.role_id
        </if>
        where m.status = 1
        <if test="userId != null and userId != 1">
            and p.status =1 and r.status = 1 and rr.type = 9 and ur.user_id = #{userId}
        </if>
    </select>
</mapper>
